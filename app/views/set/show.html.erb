<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>
<p>
<%#= @query.get_sequence %>

  <b>Query:</b>
  <%=h @query.name %>
</p>

<p>
  <b>Description:</b>
  <%=h @query.description %>
</p>

 <%  if(@query.user_id == current_user.id) %>
<%= link_to 'Change Search Parameters', edit_query_path(@query) %> |
<%= link_to 'Delete Query', query_path(@query), :confirm => 'Are you sure?', :method => "delete" %>
<%  end %>
<br/>
<%= link_to 'Download Nucleotides', "/queries/download_fasta/#{params[:id]}" %> |
<%= link_to 'Download Georefs', "/queries/download_metadata/#{params[:id]}" %> |
<%= link_to 'Download Metadata', "/queries/download_strain/#{params[:id]}" %>
<br/><br/>

<br/><br/>

<%# if @sequences.total_entries >499  %>
  <%#=  label_tag('You need less then 500 sequence to make a supra map kml and a single sequence type to make a kml file') %>

<%#  else %>
  <%#  if(@query.kml_status == 0) %>

   <%#  if(@query.user_id == current_user.id) %>
    <%#= form_tag "start_poy/#{params[:id]}" %>
    <%#= submit_tag("Create a supra map kml file") %>
   <%#  end %>
  <%#  end %>
  <%#  if @query.kml_status ==1 %>
    <%#=  label_tag('Your kml file for supra map is currently being generated please come back again soon.') %>
  <%#  end %>
  <%#  if @query.kml_status ==2 %>
      <%#=  link_to 'Download Supramap KML', "/queries/download_supramap_kml/#{params[:id]}" %> |
      <%#=  link_to 'Download Aligned Fasta Sequences', "/queries/download_aligned_fasta/#{params[:id]}" %> |
      <%#=  link_to 'Download Poy Supermap Output', "/queries/download_supramap_output/#{params[:id]}" %> |
       <%#  if(@query.user_id == current_user.id) %>
      <%#= form_tag "start_poy/#{params[:id]}" %>
      <%#= submit_tag("Re-Run Supra Map", :confirm =>'Are you sure? You only want to do this if changed your query parameters or ran supermap a while ago with now out of date data' ) %>
         <%#  end %>
    <%#  end %>
<%#  end %>
<%#*<br/><br/>%>


<h3>Listing <%#= @sequences.total_entries %> Results:</h3>

<%= @query.get_sql %>

<%#= will_paginate @sequences, :params => params %>

<div class="list">
<table>
  <tr>
    <th>Isolate Name</th>
    <th>Pathogen</th>
    <%#*<th>Genbank Accession ID</th>%>
    <th>Segment</th>
    <th>Location</th>
    <th>Host</th>
    <th>Collect Date</th>

  </tr>

<% @query.get_sequence do |sequence| %>
  <tr>
    <td><%=h sequence[:virus_name] %></td>
    <td><%=sequence[:subtype] %></td>
    <%#*<td>%>
      <%# if sequence[:genbank_acc_id] %>
        <%#= link_to sequence[:genbank_acc_id], "http://www.ncbi.nlm.nih.gov/nucleotide?term=#{sequence[:genbank_acc_id]}", :target => "_blank" %>
      <%# else %>
      <%#*Gisaid%>
      <%# end %>
    <%#*</td>%>
    <td><%= sequence[:protein] %></td>
    <td><%= sequence[:country] %></td>
    <td><%= sequence[:host] %></td>
    <td><%= sequence[:date] %></td>

  </tr>
<% end %>
</table>
</div>
